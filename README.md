# SMS-React-Twilio

## Running the project

clone the project, change into the directory and install the dependencies.

```bash
cd project-name
npm install
```

Start the application:

```bash
npm run dev
```
